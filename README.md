# Neovim Config

## Minimal structure

The only files needed to have a working lazy.nvim setup are:

```
~/.config/nvim/
├── init.lua
└── lua/
    ├── config/
    │   └── lazy.lua
    └── plugins/
        └── example.lua
```
